# Week 1 — Source evaluation

Evaluate the three model sources (1, 2, 3) as standalone predictors against ground
truth: error distributions, error by value bin / province / city / object type, a
geographic error map, and a simple ensemble comparison.

Run (from repo root):

```bash
python -m experiments.week1_source_evaluation.scripts.plot_performance
```

Outputs: `plots/` (the 01–10 figures).
